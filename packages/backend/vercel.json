{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @monorepo/shared build && pnpm --filter @monorepo/backend db:generate",
  "installCommand": "cd ../.. && pnpm install",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
